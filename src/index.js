import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
// this code is JSX, which is HTML with minor differences, that is compiled down by Babel to React.createObject calls
